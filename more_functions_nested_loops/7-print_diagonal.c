#include "main.h"

/**
 * print_diagonal - code will print diagonal line.
 * Return : always 0
 */

void print_diagonal(int n)

{
	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	print_diagonal(-4);
	return (0);
}
